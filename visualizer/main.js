/**
 * BATEC VJ ENGINE v6.0 - Modular Architecture
 * Professional Stage Projection Framework
 */

// Core components are loaded globally via <script> tags from the /js/ directory.
const engine = new BatecEngine();
UI.init(engine);
